
# Force npm usage and disable other package managers
engine-strict=true
use-npm=true
legacy-peer-deps=true

# Explicitly disable pnpm
use-pnpm=false

# Ensure lockfile is created/updated
package-lock=true
